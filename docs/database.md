# Database

The MVP repository is in memory to keep the first feature runnable without infrastructure. The future schema belongs in `database/schema/` and should persist users, workspaces, projects, generated artifacts, and audit events.
