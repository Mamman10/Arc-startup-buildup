# Testing strategy

Unit tests cover validation and deterministic analysis. Integration tests should cover HTTP routes once a server test harness is added. E2E coverage is planned for the form submission and error state.
