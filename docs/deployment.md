# Deployment

Run `npm test`, `npm run check`, then deploy as a Node 20 service with `PORT` set by the host. No secrets are required for this MVP. Re-check Arc documentation before introducing wallet, contract, or USDC features.
